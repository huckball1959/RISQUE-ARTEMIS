# ARTEMIS — Agent handoff log

Read this before any ARTEMIS networking / three-laptop work.

## Repository locations

| Role | Path |
|------|------|
| **Canonical (edit here)** | `C:\Users\huckb\OneDrive\Documents\GitHub\RISQUE-ARTEMIS` |
| **Backup only** | `X:\github\RISQUE-ARTEMIS` |

Unless the user explicitly says otherwise, **all changes go to OneDrive**. Sync to X: only when asked for backup.

After JS/CSS changes, bump the cache query string in `game.html` (e.g. `artemis-m273-…`) and remind the user to **hard-refresh all three laptops** (Ctrl+Shift+R).

## Test hardware (3 laptops)

| Laptop | Role | Player slot | Notes |
|--------|------|-------------|--------|
| **Guido** | Server / host / map | Player 1 | Runs `artemis-server`; host browser is not a “client laptop” but uses host ARTEMIS mode |
| **Mictor** | Client | Player 2 | |
| **Nooch** | Client | Player 3 | |

## Rig / defaults

- **Guido is rigged to win player selection** for now, unless the user specifies otherwise.
- Do not commit or push unless the user asks.
- Typical flow: Guido hosts → Mictor + Nooch connect as clients → lobby → playtest cycle (deploy → attack → reinforce → cardplay → income → receive card, etc.).

## UI conventions (ARTEMIS)

- **Active player** on their laptop: full phase UI (deploy panel, attack chrome, cardplay two-pane, receive-card hand + staging).
- **Spectators** (other laptops + Guido when not active): mirrored map + control voice; card backs only (never opponent card faces in CV).
- **Receive card**: dual-pane layout (hand above, staging below); staging pane should sit near the canvas foot with **30px bottom buffer** on the HUD column.
- **Guido attack dice**: when a client attacks, Guido should see live dice animation via `attack_live` / `risqueArtemisApplyHostAttackSpectator` — not local roll controls.

## Key files (quick index)

- `game.html` — script cache busting
- `js/artemis-net.js` — WebSocket, `attack_live`, `player_state`, mirror
- `js/artemis-play.js` — omni HUD, login suppression, my-turn classes
- `js/artemis-receive-card-panel.js` — receive-card mount / spectator HUD
- `js/artemis-attack-panel.js` — host attack spectator + client attack mount
- `js/game-shell.js` — public voice mirror, soft nav, receive-card CV
- `phases/reinforce.js` — reinforce compact HUD + confirm gating
- `phases/runtime-hud.js` — HUD column position (`syncPosition`)
- `game.css` — receive-card pane fill, attack spectator dice visibility

## Changelog (agents append brief notes)

- **2026-07-10 m345** — **Collapse idle empty prompt slots under CV.** After removing the DEV ROW, the `#ucp-slot-strip` still showed four dark empty placeholder buttons. Hide `.ucp-slot-empty` slots and collapse the strip when idle; it still appears for real prompts (Confirm/Cancel) and troop-transfer amount. CSS-only (`game.css`). Tab **ARTEMIS m345**. **Hard-refresh** (or Guido **REFRESH LOOK** for CSS).
- **2026-07-10 m344** — **Removed attack DEV ROW under CV.** Dropped `#attack-dev-row-strip` (DEV ROW / Q BLITZ L3 / Q BLITZ T3 / Q CAMP / visible cond number). Main toolbar above CV kept (ROLL / BLITZ / CAMP / CLR / AERIAL). COND modes still work with hidden `#cond-threshold` defaulting to stop-at **5**. Files: `phases/attack.js`, `phases/runtime-hud.js`, `game.css`. Tab **ARTEMIS m344**. **Hard-refresh all three laptops.**
- **2026-07-09 m343** — **Guido host: capture map no longer rewinds until CONFIRM.** Nooch/Mictor saw W. Australia flip correctly after a client capture, but Guido (host spectator) stayed on the pre-capture board until transfer CONFIRM. Cause: host-only dice reveal timer (`scheduleHostAttackDiceReveal`, ~520ms) kept the pre-capture `attack_live` snapshot and re-painted the map after the post-capture `attack_live` had already applied ownership. Clients never run that path. Fix: reveal timeout always paints from live `window.gameState` (`js/artemis-attack-panel.js`). Tab **ARTEMIS m343**. **Hard-refresh all three laptops** (Guido especially — JS change).
- **2026-07-09 m342** — **Attack capture clobber guard + fair random full turn order.** Client conquer (plain dice / campaign) briefly showed on spectators then reverted until CONFIRM: a delayed pre-capture `player_state` could arrive after post-capture `attack_live` and overwrite the board; host then mirrored stale state to other clients. Fix: `artemisMergePreferLiveAttackBoard` on host `player_state` apply; attack spectators reject regressive host mirrors (`risqueArtemisAttackLiveBoardRegression`); clear stale attack selection line on `pending_transfer` in `applyAttackLiveSpectator`. Fair random setup roulettes now shuffle the remaining players for `turnOrder` (not login/table order) after the winner is picked (`phases/player-select.js`). Tab **ARTEMIS m342**. **Hard-refresh all three laptops.**
- **2026-07-08 m341** — **Client attack capture sync + cardplay stale CV backs.** When a client (Nooch/Mictor) attacks, spectators (Guido + other client) lagged on territory color + min troops until transfer CONFIRM — especially campaign auto-transfer hops and when an out-of-order `attack_live` (pre-capture dice push) arrived after the post-capture push. Fixes: `applyAttackLiveSpectator` enforces monotonic `attack_live` seq, replaces full per-player `territories` arrays from the patch (not troop-only merge), keeps pending_transfer ownership pin; campaign auto-transfer now calls `pushArtemisClientAttackLiveMirror()` after each hop (`phases/attack.js`). Cardplay: spectators no longer get stale `#risque-public-cardplay-voice-backs` during host book recap animation — skip inject when `risquePublicRecapShelfAnimOwnsCardStrip`, clear backs in `artemisSyncCardplayRecapUi` (`game-shell.js`, `artemis-cardplay-panel.js`). Tab **ARTEMIS m341**. **Hard-refresh all three laptops.**
- **2026-07-06 m340** — **Postgame on all laptops: Wayback + toggles, observers sync after win.** Clients were stuck after celebration because `risqueMountImmediateGameWinOverlay` skipped navigation when `risqueDisplayIsPublic` (all ARTEMIS clients). Now client laptops advance to postgame after the win overlay; host pushes `phase=postgame` mirror; clients follow via `artemis-net` (postgame in phase-rank/follow lists). Every remaining player gets `risque-view-host` toggles in postgame (`risqueArtemisEnterPostgamePlayMode`). Wayback prep runs on host **and** client laptops (`risqueArtemisLaptopCanRunWayback`); autoplay removed — **PLAY WAYBACK** button on each machine at their own pace. ARCHIVE stays host-only. Tab **ARTEMIS m340**. **Full reload all three.**
- **2026-07-06 m339** — **Attack spectator map block + postgame landing fix.** (1) Guido/Nooch clicks during another player's attack now have **no map effect** and show a toast: *"You can not select territories. Mictor is the attacker"* (dynamic name). Uses `risqueArtemisShouldAttackChromeBeInteractive` + guards in `core.js`, attack delegate, `risqueAttackPhaseTerritoryClick`, and spectator strip handler; host map paints re-strip before every `renderTerritories`. (2) After cardplay/attack game-win celebration, host now lands on **postgame** (map + Wayback + luck toggles) instead of resurrecting the old login overlay — `isPastArtemisLoginPhase` treats `phase=postgame`, `risqueArtemisPreparePostgameEntry` strips login chrome + sets `lobbyStarted`, postgame added to same-document soft-nav. Tab **ARTEMIS m339**. **Full reload all three.**
- **2026-07-06 m338** — **Attack spectators cannot pick territories; Mictor conquer mocks carry Alaska.** Observers (host + other clients) could still highlight attack from/to on map clicks because `renderTerritories` wired SVG handlers to `risqueAttackPhaseTerritoryClick` without a turn check. Guard at top of `phases/attack.js` `risqueAttackPhaseTerritoryClick`: return unless `risqueArtemisIsMyTurn`. Conquer attack saves for Mictor chains: her 4-card hand now includes **alaska** (Guido no longer holds duplicate alaska card). Card-win save `conquer-mictor-nooch-cardplay` unchanged (Mictor already had alaska in 9-card hand). Tab **ARTEMIS m338**. Reload all three.
- **2026-07-06 m337** — **Cardplay CV hints + conquer card-win mocks + game-win postgame fix.** (1) ARTEMIS cardplay instructions moved to control voice (`ucp-voice-report--cardplay-instruction`, 12px); panel hints hidden for more card room. (2) Conquer test menu: 4 chains + **From attack** / **Card win** entry; new `conquer-nooch-guido` attack save + four `*-cardplay.json` saves (9-card hand = 2 valid books + finisher territory card, e.g. Mictor + Alaska vs Guido). (3) Cardplay elimination: game win → confetti overlay → **postgame** on map (not stale login / old `?phase=conquer` hop); mid-chain elim → inline `risqueConquerStartEliminationFlow`. Tab **ARTEMIS m337**. **Full reload all three.**
- **2026-07-06** — **Conquer mock saves: valid hand counts.** Attack-phase mocks had attackers at 5 cards (illegal — 5+ forces cardplay before attack). `scripts/gen-conquer-saves.js` now asserts **attacker 4**, **each defender 5**; regenerated all three JSON saves. Re-pick conquer test from host menu to load fresh JSON.
- **2026-07-05 m336** — **Conquest elimination: celebratory full-screen overlay + active-player roster fade.** On every player elimination (attack → conquest flow), all screens now get a gold-flash backdrop panel with big “X HAS CONQUERED Y” headline, CSS sparkles + confetti burst (CDN fallback: sparkles only), and an **Active players** chip list — conqueror highlighted, defeated name **fades/strikes through and collapses** off the list after ~1s. Host gets a green **Proceed to take cards** button on the overlay (and in control voice) after 3.5s. Synced to clients/public via existing `risqueConquerSyncCelebrationFromState` mirror path. Files: `phases/conquer.js`, `game.css`, `game.html`. Tab **ARTEMIS m336**. **JS change — full reload all three machines.**
- **2026-07-04 m335** — **Cardplay upper hand: 5×2 grid (correct phase).** User still saw hidden cards after m333/m334 because those fixed **receive-card** (`#receivecard-hand-strip-upper`), but the conquer→continue bug is **cardplay** (`#cardplay-card-grid`): flex-wrap + fixed 120×186 / 213px slots clipped row 2 behind `overflow:hidden` on the 50% upper pane — processing a book reduced count and “revealed” cards. Fix: (1) `#cardplay-card-grid` → 5-column CSS grid, 2 rows at 4+ cards; (2) `cardplaySyncUpperHandLayout()` in `phases/cardplay.js` measures pane height and sets per-row `maxHeight` + `--cardplay-hand-row-h`; (3) override ARTEMIS 213px slot/card rules inside `#cardplay-card-grid` only; (4) hand-stack `overflow-y: hidden` (no scrollbar clip). REFRESH LOOK re-syncs cardplay hand after CSS reload. Tab **ARTEMIS m335**. **JS change — full reload all three (`risqueArtemisDevReload()` on Guido or Ctrl+Shift+R).** Verify: `document.querySelector('meta[name="risque-artemis-build"]')?.content` → `m335-cardplay-upper-two-rows`; on cardplay screen `typeof cardplaySyncUpperHandLayout` → `"function"`.
- **2026-07-04 m334** — **Receive-card upper pane: forced 2 rows (4+ cards), measured thumb heights.** m333 grid still showed only ~4 cards because `.receivecard-compact-root--simple .receivecard-thumb` locked **213px height** + `flex-shrink:0`, overflowing the 50% pane. Fix: (1) override fixed 138×213 inside upper/staging strips; (2) **2 rows at 4+ cards** (5 cols × 2 rows = up to 9); (3) `receiveCardSyncUpperHandLayout` measures strip height and sets per-row `maxHeight` inline + `--receivecard-upper-row-h`; (4) REFRESH LOOK now calls `receiveCardRunDisplay()` after CSS reload. Tab **ARTEMIS m334**. **Guido: `risqueArtemisDevReload()` once** (Ctrl+Shift+L is CSS-only on host).
- **2026-07-04 m333** — **Receive-card / conquest upper pane: fit up to 9 cards without overlap.** After the staging→hand fly-up merge, the upper window showed only ~4 fixed-size (102–138px) thumbnails with the rest clipped/eclipsed (`overflow:hidden` + inflexible heights). Fix: 5-column CSS grid on `#receivecard-hand-strip-upper` with 1 row (≤5 cards) or 2 rows (6–9); thumbs scale with `max-height:100%` / `object-fit:contain` inside the 50% pane. Removed conquest-elim fixed 102×157 override. `phases/receivecard.js` stamps `data-receivecard-upper-count` + row classes after paint/merge. Tab **ARTEMIS m333**. **Hard-reload all three machines** (CSS + receivecard.js).
- **2026-07-04 m332** — **Conquer mock: Guido still had toggles-only HUD (no dice/ROLL).** m331 fixed turn detection but the real blocker was the login minimal HUD shell: soft-nav to attack stripped `runtime-hud-root--login` without rebuilding DOM, so `runtimeHud.ensure()` saw a "full" root (no login/setup class) and skipped — leaving toggles + stats only, no `#control-voice` or attack toolbar. Clients showed "GUIDO ATTACK" via mirror; host never got attack chrome. Fix: (1) `phases/runtime-hud.js` — `ensure()` only skips rebuild when `#control-voice` + `#hud-main-panel` exist. (2) `js/game-shell.js` — attack soft-nav calls `runtimeHud.ensure()` when login or incomplete shell. (3) `js/artemis-attack-panel.js` — `attackControlsReady()` (requires `#roll` + `__risqueAttackInitialized`); force HUD shell + canvas visible before mount; `finishAttackMountInit` retry. (4) `js/artemis-auto-save.js` — conquer boot recovery also restores canvas + incomplete HUD. Tab title **ARTEMIS m332**. **Full reload Guido (Ctrl+Shift+R) before retest.**
- **2026-07-04 m331** — **Conquer mock: Guido (host) had no attack controls.** Map/state loaded but HUD showed omni toggles only — host was stuck in attack *spectator* mode (toolbar hidden by CSS) because `risqueArtemisIsMyTurn` for attack only compared roster slots, unlike deploy which also accepts `hostSessionName === currentPlayer`. Conquer boot often lands before roster/control slot is fully aligned, so slot check failed even though Guido was the active player. Fix: (1) `js/artemis-play.js` — same host-name shortcut for attack/reinforce/receivecard/cardplay/income as deploy. (2) `js/artemis-auto-save.js` — conquer merge stamps default `artemisRoster`, clears setup-deploy winner lock, schedules `risqueArtemisEnsureAttackInteractive` retry after soft-nav. (3) `scripts/gen-conquer-saves.js` — saves now embed `artemisRoster`, `artemisControlSlot`, `risqueArtemisControlSeq`. Tab title **ARTEMIS m331**. **Full reload Guido before retest.**
- **2026-07-04 m330** — **Conquer-test mock saves + host menu mode 4.** Three attack-phase saves (`conquer-guido.json`, `conquer-mictor-guido.json`, `conquer-mictor-nooch.json`) position each victim on one border territory (1 troop) with the attacker adjacent and stacked — one attack triggers celebration → card transfer. Victim hands carry 3–5 cards for transfer testing. Generator: `scripts/gen-conquer-saves.js`. Host test launcher adds **4 Conquer test** with sub-pick: Guido→Mictor→Nooch / Mictor→Guido→Nooch / Mictor→Nooch→Guido. `js/artemis-auto-save.js` now boots any `artemisAutoSave` save (cardplay or attack) via `risqueArtemisAutoSaveBoot`; conquer saves keep their phase/currentPlayer. Files: `js/artemis-login.js`, `js/artemis-auto-save.js`, `game.css`, `game.html`, three JSON saves. Tab title **ARTEMIS m330**. **JS change: full reload all three machines.** Not yet done: unified final-win ceremony, two-pane conquest card exchange UI.
- **2026-07-04 m329** — **Fix book-turn income grid blank on all screens (hand fix from m328 kept).** m328 retest (fresh login, Nooch first book): cards in hand correct after receive-card, deploy banked +19 correctly, but income showed only `NOOCH — INCOME` with no territory/book/continent lines on host + both clients. Diagnostics (`income_breakdown_probe` seq 21): `bookPlayedThisTurn:true`, `bookValue:1`, but `hasBreakdown:false` everywhere; Nooch's console showed incomeInit computed total=19 with book +10, yet CV was overwritten to the fallback before/after mount. Root cause: race — stale mirrors arrive at income without `risquePublicIncomeBreakdown`; m328's book-commit guard deleted breakdown when fixing stale book flags; `risqueArtemisSyncPhaseControlVoice` painted the empty fallback over a briefly-correct grid. Fix: new `risqueArtemisEnsurePublicIncomeBreakdown(gs)` in `js/artemis-income-panel.js` (synchronous breakdown builder from live state, including book bonus). Called from host `finalizeHostClientState`, client income mirror apply, `ensureIncomeBreakdownVoice`, and `risqueArtemisSyncPhaseControlVoice` before fallback. Book-commit guard now only clears breakdown when hand cards regress (`removedAgain`); when only book flags were stale during income it rebuilds breakdown instead. `risquePublicMirrorGameStateApply` preserves local breakdown on active client when incoming mirror lacks it. Bumped build to m329; cache-busted `artemis-income-panel.js`, `artemis-net.js`, `artemis-play.js`, `game-shell.js`; `profiles.json` → m329. **JS change: full reload all three machines before retest.**
- **2026-07-04 m328** — **Fix client book-cash stale mirror regression (cards return + no book income).** Diagnostics confirmed one root cause: when a client cashes a book, the local client correctly removes the 3 cards (`cardplay_book_removed`: hand 3→0, `bookPlayedThisTurn:true`), then within seconds an incoming stale mirror restores the pre-book hand and clears book flags (`cardplay_host_recap_push`: hand back to 3, `bookPlayedThisTurn:false`). That makes receive-card show the spent cards again and makes income drop the book bonus / sometimes show only `PLAYER — INCOME`. Host/Guido worked because host state is authoritative and was not overwritten by a mirror. Fix: in `phases/cardplay.js`, stamp `window.__risqueArtemisLocalBookCommit` at the exact book-removal moment (player, removed card ids/names, post-book hand, bookValue). In `js/artemis-net.js`, before applying any incoming public mirror on clients, `artemisProtectLocalBookCommitMirror()` removes those spent card ids/names from the incoming player hand and preserves `bookPlayedThisTurn`/`bookValue` for cardplay→income; it also deletes stale income breakdowns so income remounts from the corrected state. In `js/artemis-preset.js`, the hand hydrator now respects that same stamp and refuses to rebuild a just-spent book hand from stale `cardCount`. Added `book_commit_guard` / `book_commit_hand_guard` diagnostics. Bumped build to m328, cache-busted `artemis-net.js`, `artemis-preset.js`, and `phases/cardplay.js`; updated `profiles.json` cacheVersion and all normal/cardplay-test `v` params to m328. **JS change: full reload all three machines before testing.**
- **2026-07-03 m327** — **Two new live-game bugs after a book is cashed (probe added to diagnose).** In a full real game (no rig/mock), the turn where the current player (Mictor, went first) CASHES A BOOK in cardplay lands on income showing only "MICTOR - INCOME" with **no numbers on every screen** — the continue button still banks the correct total (28 = N.America 10 + S.America 8 + book 10). Other players' income renders fine. Second bug (regression, "we've been here before"): at that player's receive-card, **the just-played book cards are back in hand**; Guido's hand (next player) is correct. Both smell like a stale gameState snapshot lacking the post-book mutations being merged for the book-player (would wipe `risquePublicIncomeBreakdown` AND restore `player.cards`). This is a distributed host/client sync timing bug that only reproduces live, and the hand bug has regressed before — so rather than blind-patch the sync code (risking the working players), added a probe. New `income_breakdown_probe` in `artemis-diagnostics.js` (fires on host + clients every 500ms while phase is income/con-income, on change): records whether `gs.risquePublicIncomeBreakdown` exists + its `total`/`bookBonus`/`continentRows`, `bookPlayedThisTurn`, the player's `bookValue`, `handCount`, and whether `#control-voice-text` actually painted the grid (`cvTextHasGrid`) — so the report tells us STATE-missing vs DOM-not-painted, and whether the hand is already wrong at income time (linking to the receive-card bug, which existing `receive_card_snapshot` already captures via `handNames`). **Action: reproduce once (fresh real game, first player cashes a book), then say "read diagnostics" — the server writes `logs/artemis-last-report.json`.** No behavior change this build. Bumped build to m327, diagnostics `?v=` to `artemis-m327-income-breakdown-probe`, `profiles.json` cacheVersion + cardplay-test `v` to m327.
- **2026-07-03 m326** — **ROOT CAUSE of the "truncated attack CV that only fixes after a reload".** User discovered the attack dice results are truncated on a *fresh* mock game (deploy→attack) but perfect after tapping browser reload. That ruled out a CSS-specificity problem (same CSS both times) and pointed at runtime state. Found it: `artemis-deploy-panel.js` `risqueArtemisEnsureDeployOwnerVoiceChrome` stamps **inline** `height/min-height/max-height:168px !important` (+ font vars + `ucp-control-voice--artemis-deploy-owner` class) on `#control-voice` for the active deployer. **Inline `!important` beats stylesheet `!important`**, so the 168px stamp survives deploy→attack and pins the CV short — the m323/m324 280px rule can't win. A reload straight into `?phase=attack` never passes through deploy, so no stamp → the CSS wins → "perfect". Fix (`artemis-play.js`): added `artemisClearDeployOwnerVoiceStamp()` and call it from `artemisStampOmniHudDocumentClasses` (runs every render) whenever `phase !== "deploy"` — removes the inline height/font props + marker class from `#control-voice`/`#control-voice-text`/`#control-voice-report`/`.ucp-voice-messages`, scoped to the deploy-owner marker class so nothing else is touched. Now the stamp is cleared on entry to attack (and income/reinforce/etc.), letting each phase's own CSS render correctly on the FIRST paint. Keeps m323/m324 CSS. **This is a JS change → needs one full reload on each machine to load (REFRESH LOOK won't pick up JS).** Bumped build to m326, `artemis-play.js ?v=` to `artemis-m326-clear-deploy-voice-stamp`, `profiles.json` cacheVersion + cardplay-test `v` to m326.
- **2026-07-03 m325** — **Host "REFRESH LOOK" button — reload graphics without restarting the game.** User: full browser refresh on the server (Guido) bounces back to login; clients can hard-refresh fine. A desktop batch file cannot reload CSS in an already-open tab, so added a persistent host-only control instead. New `js/artemis-host-refresh-look.js`: injects a green **REFRESH LOOK** button into the fixed ARTEMIS top bar (next to Fullscreen), reloads `game.css` with a live cache-bust (`?v=live-<timestamp>`), shows a bottom toast "Graphics refreshed — game still running", and calls `risqueArtemisScheduleLayoutSync` when available. On the host only, **Ctrl+Shift+L** now does the same CSS-only refresh (captures before `game-shell.js` full `artemisDevReload` — avoids the sign-in trap). Clients unchanged. Top-bar hint updated. CSS for button + toast in `game.css`. Bumped build to m325, `game.css ?v=` to `artemis-m325-host-refresh-look`, `profiles.json` cacheVersion + cardplay-test `v` to m325. **One normal reload needed on Guido to get the button; after that, tap REFRESH LOOK for CSS fixes.**
- **2026-07-03 m324** — **Host (Guido) active attacker got the same CV fix.** After m323 fixed Mictor + Nooch (clients), Guido's own attack turn still showed no dice-roll result in the control voice — because the m323 selector targeted `html.risque-artemis-client`, and the host uses `html.risque-artemis-host`. Extended the m323 rule in `game.css` to also match the host, scoped with `body[data-risque-phase="attack"]:not(.risque-artemis-attack-spectator)` so the CV only grows to 280px when Guido is *actually attacking* — not when he's spectating another player's attack (host spectator adds `body.risque-artemis-attack-spectator`, toggled off in `artemis-attack-panel.js` when he becomes active). Same box: `height/min/max:280px; overflow:hidden; flex-shrink:0` on `#control-voice`, and `min-height:0; overflow:hidden; justify-content:flex-start` on inner `.ucp-voice-body`/`.ucp-voice-messages`. Client rule unchanged. Bumped build to m324, `game.css ?v=` to `artemis-m324-host-attacker-voice-height-fix`, `profiles.json` cacheVersion + cardplay-test `v` to m324. **Hard-refresh Guido (Ctrl+Shift+R).**
- **2026-07-03 m323** — **Found why the CV never grew: rules required `risque-artemis-my-turn`, absent during a client's active ATTACK.** m320/321/322 all scoped `#control-voice` height to `html.risque-artemis-client.risque-artemis-my-turn body[data-risque-phase="attack"]`. Read m322 report: `#control-voice` still measured **~89px** even with `min-height:280px !important` + `flex-shrink:0` — a matched rule would force ≥280, so the selector was NOT matching. `data-risque-phase` is set from `gs.phase` (fine), and `risque-artemis-client` is stable, so the culprit is `risque-artemis-my-turn`: it's added on `<html>` during cardplay/deploy (where the m318 recap fix used it successfully) but is evidently NOT present during the client's active attack. Fix (`game.css`): dropped `.risque-artemis-my-turn` from the attack CV rules → `html.risque-artemis-client body[data-risque-phase="attack"] #control-voice.ucp-control-voice { height/min/max:280px; overflow:hidden }`. This reliably matches the active attacker (also spectator clients, harmless — taller CV just shows the mirrored line). Also hardened the m319 `attack_voice_probe` to record `htmlClass`, `bodyPhaseAttr`, `cvClass`, and computed `cvMinHeight`/`cvHeightCss` so the mismatch can never be a mystery again. Bumped build to m323, `game.css ?v=` to `artemis-m323-attacker-voice-height-fix`, diagnostics `?v=` to `artemis-m323-attack-voice-probe-2`, `profiles.json` cacheVersion + cardplay-test `v` to m323.
- **2026-07-03 m322** — **Active-attacker CV: reverted m321 overflow:visible (caused text overlap), used a bigger fixed box.** User: m321 made the text overlap and the window didn't visibly grow. `overflow:visible` + `height:auto` let the inner voice stack spill/overlap instead of the box growing. Reverted to the m320 known-good structure (fixed box + `overflow:hidden`, inner body/messages `min-height:0; overflow:hidden; justify-content:flex-start`) and simply stretched further to a **fixed 280px** (`height/min-height/max-height: 280px`). Fixed height is more predictable than `auto` (which wasn't growing) and guarantees a visibly bigger window; the taller box pushes the slot strip / dev row / combat log / card-earned window down the flex column. Still scoped to `html.risque-artemis-client.risque-artemis-my-turn body[data-risque-phase="attack"]`. If 280px is too tall/short, it's a one-number tweak. Bumped build to m322, `game.css ?v=` to `artemis-m322-attacker-voice-height-3`, `profiles.json` cacheVersion + cardplay-test `v` to m322.
- **2026-07-03 m321** — **Active-attacker CV result now fully visible (m320 follow-up).** User confirmed m320 was on the right track — the result text appeared but was still truncated at the bottom of the control-voice window. Content on the active attacker is dropdown + condition tally + (up to 2-line) outcome + dice report, which exceeds ~200px. Fix (`game.css`): raised the active-attacker `#control-voice` `min-height` 200px → **258px**, switched its `overflow` to **visible** (never clip), and added `#control-voice-text` / `#control-voice-report` to the `min-height:0; max-height:none; overflow:visible` set so no inner element clips. The taller box shoves the slot strip / dev row / combat log / card-earned window (`#risque-host-deck-earned-visual`) down the flex column into the panel-foot slack, per the user's request. Still scoped to `html.risque-artemis-client.risque-artemis-my-turn body[data-risque-phase="attack"]` only (spectators untouched). Bumped build meta/title to m321, `game.css ?v=` to `artemis-m321-attacker-voice-height-2`, `profiles.json` cacheVersion + cardplay-test `v` to m321. m319 `attack_voice_probe` still live.
- **2026-07-03 m320** — **Active attacker (Mictor) battle-result readout was clipped, not missing — fixed.** Read m319 `attack_voice_probe` in `logs/artemis-last-report.json`: on the 9:45:39 roll Mictor's own client had `risqueAttackOutcomePrimary = "MICTOR AND NOOCH EACH LOSE 1."` and `#control-voice-text.innerHTML = "<strong>MICTOR AND NOOCH EACH LOSE 1.</strong><br>Refer to the buttons above…"` (h≈40, display block, visible) — so the outcome IS produced on the active client (user's "no results anywhere" was really "clipped out of view"). The container `#control-voice` measured only **~80px** with `overflow:hidden`, and on the active attacker that box also holds the injected `ATTACKER DICE` dropdown (`#control-voice-extras`) + condition tally (`0 until condition is met`), which eat the visible 80px → the outcome text renders below the fold. Spectators (Guido/Nooch) have no dropdown/tally so their 80px box shows the result — exactly why it appeared in their CV only. Note base `.ucp-control-voice` is 180px (would fit); an attack-context override squeezes the client box. Fix (`game.css`, appended): high-specificity rule for the active attacker only — `html.risque-artemis-client.risque-artemis-my-turn body[data-risque-phase="attack"] #control-voice.ucp-control-voice { height:auto; min-height:200px; overflow:hidden }` plus `.ucp-voice-body/.ucp-voice-messages { min-height:0; overflow:visible; justify-content:flex-start }`. Spectator clients lack `risque-artemis-my-turn`, so they're untouched. Extra height comes from the slack above the card-earned window (`#risque-host-deck-earned-visual`) at the foot of the HUD column (flex-column stack: attack chrome → control-voice → slot strip → dev row → log → reinforce footer + card-earned). Kept the m319 `attack_voice_probe` live so this run's report confirms `#control-voice` height grows and the outcome is on-screen. Bumped build meta/title to m320, `game.css ?v=` to `artemis-m320-attacker-voice-height`, `profiles.json` cacheVersion + cardplay-test `v` to m320. (Diagnostics `?v=` stays m319 — unchanged this build.) **Also still outstanding:** repeated `[Core] Error rendering territories: risqueSvgNs is not defined` on Mictor's console during attack — not blocking the readout, but worth fixing next.
- **2026-07-03 m319** — **Active-attacker sees no battle outcome text — measurement probe.** After m318 (recap windows fixed), user reports: Mictor (active client) sees her dice fine, but no result line like "MICTOR LOSES 2" / "GUIDO LOSES 1 AND MICTOR LOSES 1" anywhere on her screen; spectators (Guido host + Nooch) show it in Control Voice fine. Traced the active-player path: on a single roll, `phases/attack.js rollDice()` → `applyBattleRoundAfterRoll()` sets `risqueAttackOutcomePrimary/Report` and calls `setControlVoiceText(outcomeU, …)` (attack.js ~2359), then `showPrompt()` writes `<strong>MICTOR LOSES 2</strong>…` into `#control-voice-text` (attack.js ~1694). So the active client *should* show it in the same CV. Spectators paint it via `risquePublicApplyVoiceAndLogMirror()` `useBattleReadout` branch (game-shell ~5783). `js/artemis-play.js risqueArtemisSyncPhaseControlVoice()` attack branch (~1630) also calls `risquePublicApplyVoiceAndLogMirror(gs)` for the active client — which repaints CV from `risqueAttackOutcomePrimary`; if that field is empty at reconcile time it would fall through and wipe the outcome. Rather than guess which reconcile clears it, added an `attack_voice_probe` in `js/artemis-diagnostics.js` (m319): on the active client in attack, every 400ms (deduped on change) it logs `#control-voice`/`#control-voice-text`/`#control-voice-report` (text, innerHTML, computed display/visibility/opacity, rect) plus `risqueAttackOutcomePrimary/Report`, `risqueControlVoice`, `campaignActive`, `displayIsPublic`. This will show the outcome getting set and then wiped (and by what state). Bumped diagnostics `?v=` to `artemis-m319-attack-voice-probe`, build meta/title to m319, `profiles.json` cacheVersion + cardplay-test `v` to m319. **Next: user runs a Mictor attack, then read `logs/artemis-last-report.json` `attack_voice_probe` events.**
- **2026-07-03 m318** — **Mictor active-player recap windows finally targeted by measurement.** User confirmed m317 and ran the mock. Read `logs/artemis-last-report.json` `cardplay_recap_layout` events: Mictor/P2 active view had `htmlClass="risque-artemis-client risque-view-host risque-artemis-my-turn"` and root `--host-cardplay-recap`; viewportH 607, root rect h 562. The decisive difference from Nooch/P3 spectator: Mictor's `#hud-main-panel` measured only **311px** high and computed `flex: "0 0 auto"`, so overlay/shelf were ~303px and panes ~148px each. Nooch's spectator `#hud-main-panel` measured **634px** and computed `flex: "1 1 0px"`, giving panes ~235px each. Cards were already comparable, matching the user's observation. Root cause: the later generic ARTEMIS cardplay rule `html.risque-artemis-client .runtime-hud-root--artemis-cardplay #hud-main-panel { flex: 0 0 auto !important }` beat the earlier active-client host-recap fill rule. Fix (`game.css`): appended a tight active-client override for `html.risque-artemis-client.risque-view-host.risque-artemis-my-turn #runtime-hud-root.runtime-hud-root--artemis-cardplay.runtime-hud-root--cardplay-panel-only.runtime-hud-root--host-cardplay-recap #hud-main-panel` setting `flex: 1 1 0 !important`, `height:auto`, `min-height:0`, `max-height:none`, `overflow:hidden`, `padding:8px`, plus matching overlay/shelf `flex:1 1 0` / `height:100%` / `max-height:100%`. This should make Mictor's two card windows consume the same remaining column height path as the spectators. Files: `game.css`, `game.html`, `launchers/profiles.json`. Tab title **ARTEMIS m318**. **Next:** retest Mictor book animation; if the windows now grow too tall or crowd CV, lower `#hud-main-panel` flex back to `1 1 auto` or cap overlay with `max-height`.
- **2026-07-03 m317** — **STOP guessing — ground-truth layout probe (Mictor's animation).** m315 AND m316 produced "no change at all" on Mictor even though m316 edited the exact same pane rules m314 (which worked) touched. Decisive check of `logs/artemis-last-report.json`: Mictor (slot 2 / c2) logged `artemis-build-loaded { build: "m316-…" }`, so **it is NOT a cache miss — she is running m316 and the CSS is inert.** That means the shelf panes/cards are not the element actually deciding her window height, or a higher-specificity/inline constraint wins — I've been styling the wrong node. Also found the **diagnostics script was never cache-busted** (`artemis-diagnostics.js?v=artemis-m213`), so any probe I add would be served stale — bumped it to m317. Added a real measurement instead of another blind CSS edit: `measureRecapShelfLayout()` in `js/artemis-diagnostics.js` reports the live `getBoundingClientRect` height/width + computed `min/max/height/flex/display/padding` and class names for `#runtime-hud-root`, `#hud-main-panel`, the recap overlay, the `--shelf` panel, both `.cp-shelf-upper/-lower` panes, a lower card, and the upper processing card. It rides the existing 8s `cardplay_client_snapshot` AND a new 1.2s `cardplay_recap_layout` probe that fires only while the recap overlay is on screen (so the few-second animation isn't missed), de-duped on change. **No CSS/layout change this build** — purely instrumentation. **Next:** user runs the mock (Mictor's turn), then read `logs/artemis-last-report.json` → find the `cardplay_recap_layout` events for slot 2; the node whose measured `h` is small while its parent is tall is the real constraint, then fix THAT element. Files: `js/artemis-diagnostics.js`, `game.html`, `launchers/profiles.json`. Tab title **ARTEMIS m317**.
- **2026-07-03 m316** — **Why m315 did nothing + the real "fill remaining space" fix.** User: "did NOT notice any change" after m315. Diagnosis: for the ACTIVE CLIENT (Mictor) the recap overlay rule (m305, `html.risque-artemis-client … .recap-overlay--in-panel`) set `height: auto !important`. With an auto-height overlay, the shelf's `height: 100%` cannot resolve, so the shelf collapsed to CONTENT height (= the panes' `min-height`) and sat centered — leaving dead space around it. That's why pane `min-height` (m314) was the ONLY thing that ever changed the windows, and m315's padding/top/card-cap changes were inert (the panes stayed pinned at their floor and never grew into the reclaimed space). Fix (`game.css`): gave the overlay a resolvable height — active-client overlay `height: auto` → `height: 100% !important`, and added `height: 100%` to the general host-recap overlay rule too. Now the shelf fills `#hud-main-panel` and the panes flex-grow to fill the column (like the spectator windows do), so m315's padding reclaim + card caps finally take effect. Also nudged the pane floor 44vh → 46vh. Active players + host spectator; public TV untouched. CSS-only. Files: `game.css`, `game.html`, `launchers/profiles.json`. Tab title **ARTEMIS m316**. **Verify tab title reads m316** after hard-refresh — if it still says m315, the CSS didn't reload (cache), which would also explain "no change". **Next:** if now too big/clipped, lower pane floor + card caps; if still not filling, check for JS inline height on the overlay/shelf.
- **2026-07-03 m315** — **Recap windows fill remaining space + much bigger cards.** m314 doubled the windows ("much better"), but the user wants them to fill the space under the control-voice down to the canvas (like spectators do) with bigger cards, without spilling off-canvas. Mechanics: the two panes flex-fill the shelf, the shelf fills `#hud-main-panel` (active client shelf is `min-height:120px / max-height:100%`, m305), so the remaining limiters were (a) `#hud-main-panel`'s 25px padding eating height + width, (b) the JS-aligned column top, and (c) card caps of `min(26-28vh, ~186-200px)`. Fix (`game.css`, appended override block, last + `#runtime-hud-root` specificity so it wins): tightened recap `#hud-main-panel` padding 25px → 8px; raised lower shelf cards to `clamp(110px, (100%-24px)/3, 240px)` / `max-height min(42vh, 340px)` and the upper processing card (both `-recap-card-img` and `-book-voice-card-img` variants) to `max-width 240px` / `max-height min(40vh, 320px)`; pulled the recap column `top` 44px → 20px for a bit more height. Scoped to active players (`--host-cardplay-recap`: Guido/Mictor/Nooch) + host spectator (`--public-cardplay-recap`); public TV untouched. Everything stays within the canvas-pinned column. CSS-only. Files: `game.css`, `game.html`, `launchers/profiles.json`. Tab title **ARTEMIS m315**. **Next:** if cards spill/clip, lower caps to ~36vh; if still short, drop padding to ~4px and/or hide the RISQUE title-stack during recap to reclaim its strip.
- **2026-07-03 m314** — **Recap windows STILL not taller (m313 was a no-op) — found the real cap.** User reported m313 changed nothing for Mictor. Decisive deduction: m312 (pane `min-height`) visibly helped, but m313 (shelf `min-height` 90vh + reclaimed column `top`) did nothing — so the panes were NOT limited by shelf/column height; they were clamped by the base rule `.recap-panel--shelf .cp-shelf-upper/.lower { max-height: calc((100% - gap)/2 - 10px) }` (≈ half the shelf, ~35vh). The empty-upper staging rules already set `max-height: none`, but the m312 *animating-state* rules (`--host-cardplay-recap .cp-shelf-lower/.upper`) inherited the half-cap — so the moment the card flew up, each window was re-clamped to ~half-shelf regardless of how tall the shelf got. Fix (`game.css`): (1) raised all seven recap pane floors from `min(260px, 30vh)` to `min(460px, 44vh)`; (2) added `max-height: none` to the two m312 animating-state pane rules so the half-cap no longer clamps them. Now both windows fill to ~44vh each (≈ double). Applies to active players (`--host-cardplay-recap`: Guido/Mictor/Nooch) and the host spectator (`--public-cardplay-recap`); public TV untouched. m313's taller shelf + `top: 44px` stay (they give the 44vh panes room). CSS-only. Files: `game.css`, `game.html`, `launchers/profiles.json`. Tab title **ARTEMIS m314**. **Next:** if a hair too tall / clipped, drop pane floor to ~40vh; if still short, the cap is fully gone so raise floor toward ~46vh.
- **2026-07-03 m313** — **Recap windows too short — make them ~2x taller.** After m312 stopped the mid-animation collapse, the user said the panes are steady but "could literally be twice as tall." Root cause: the two shelf windows already fill the shelf (each capped at ~half the shelf via `max-height: calc((100% - gap)/2 - 10px)`), and the shelf fills the HUD column — but `syncPosition()` in `phases/runtime-hud.js` pins the column `top` to the STATS table (~243px down the 1080 canvas), and stats + phase rows are HIDDEN during recap, so that entire top strip was dead space and the windows only got the lower ~72vh. Fix (CSS, `game.css`): (1) reclaim the strip — `top: 44px !important` on the recap column (`--host-cardplay-recap` for active players + `--public-cardplay-recap` for the host spectator); CSS `!important` beats the JS inline top and is scoped to recap so normal cardplay input keeps its stats-aligned position. (2) Raise the shelf `min-height` from `min(720px, 72vh)` to `min(980px, 90vh)` (both the active-host and host-spectator shelf rules) so the panes fill the reclaimed height. Public TV (`.risque-view-public`) untouched. CSS-only. Files: `game.css`, `game.html`, `launchers/profiles.json`. Tab title **ARTEMIS m313**. **Next:** if still not tall enough, drop `top` toward ~20px and/or push shelf `min-height` to ~96vh; if too tall/clipped, raise `top` back toward 80px.
- **2026-07-03 m312** — **Active-player (Mictor) cardplay recap panes collapse mid-animation.** After m311 fixed Guido's spectator view, the user watched the *active client* (Mictor) turn in a book: both shelf windows started merely undersized (cards truncated but placed right), then the moment the animation began the UPPER pane went "ridiculously short" and both were far too short to see the animation. Root cause (CSS, `game.css`): the active player (host Guido or active client Mictor) uses `--host-cardplay-recap`. m302 added a "persistent 50/50 floor at ALL times", but two later, equal-specificity rules — `.risque-public-cp-shelf-lower { min-height: 0 }` and `.risque-public-cp-shelf-upper { min-height: 0 }` (from m301, to keep panes equal) — silently override it. While the upper pane is empty the `:has(upper-content:empty)` floor rule (higher specificity) wins, so it only *looked* undersized; the instant the card flies up that guard drops and the two `min-height: 0` rules let BOTH panes collapse toward content height (upper → the tiny in-flight card). Fix: set BOTH panes' floor to `min-height: min(260px, 30vh)` (was `0`) — keeps them equal (m301's goal) AND non-collapsing (m302's goal), matching the empty-upper staging frame so there's no jump. Applies to any active player on `--host-cardplay-recap` (Guido active + Mictor/Nooch active); the public TV (`--public-cardplay-recap` / `.risque-view-public`) is untouched. CSS-only. Files: `game.css`, `game.html`, `launchers/profiles.json`. Tab title **ARTEMIS m312**. **Next:** if panes are still too short on Mictor's laptop, raise the pane floor or the shelf `min-height`.
- **2026-07-03 m311** — **Host (Guido) spectator cardplay animation truncation fix.** After m310, a mock game launched fine (Mictor starting) and the fly animation looked perfect on the clients (Mictor/Nooch). But on Guido's screen, when a client turns in a book, the **lower recap window opened too short** — book cards truncated with scrollbars — then snapped to the correct size once the animation began. Root cause (CSS): the host renders the shelf via `risquePublicRecapShelfHudClass()`, and while spectating a client it can be in `runtime-hud-root--public-cardplay-recap` (host is `risque-view-host` + public recap). The **active-host** recap (`--host-cardplay-recap`) has overlay flex-fill, a `min-height: min(720px,72vh)` shelf floor, and `min(260px,30vh)` pane floors — but the **host public** recap path had **none of these**, plus the observer empty-upper rule pinned panes to `min-height: 0`. So the shelf collapsed to content height on the first paint → lower pane too short → `.risque-public-cp-shelf-lower { overflow:auto }` threw scrollbars until a reflow. Fix: in `game.css` added a host-scoped mirror of the active-host recap sizing for `html.risque-artemis-host.risque-view-host .runtime-hud-root--public-cardplay-recap` (overlay flex-fill, shelf `min-height`, pane floors, and an empty-upper override with higher `#runtime-hud-root` specificity). Scoped to the host only — the real public TV (`.risque-view-public`) keeps its 50/50 observer behavior. CSS-only change. Files: `game.css`, `game.html`, `launchers/profiles.json`. Tab title **ARTEMIS m311**. **Next:** user re-tests Guido spectator, then we scrutinize Mictor's own animation.
- **2026-07-03 m310** — **THE fix** for "Launch does nothing / loops to sign-in." Decisive evidence from `logs/artemis-last-report.json` (m309 was loading fine): click telemetry showed `#risque-artemis-host-launcher-go` (Launch) → `no response (no_visible_change)`, phase stayed `login`, and NO events after the click — the game never started for ANY mode (not Mictor-specific). Root cause: in `commitHostLauncherChoices` I called `hideHostTestLauncher()` (which sets `hostLauncherContinueFn = null`) BEFORE `var cont = hostLauncherContinueFn` — so `cont` was always null and `runHostStartGame` was never invoked. Fix: capture `cont` first, null the field, THEN hide. One-line ordering fix in `js/artemis-login.js`. The m309 mock-nav + cardplay-recovery diagnostics stay in as guard rails. Files: `js/artemis-login.js`, `game.html`, `launchers/profiles.json`. Tab title **ARTEMIS m310**.
- **2026-07-03 m309** — Mock "loops to sign-in" when a **non-host player is chosen to start** (e.g. Mictor). Badge fix (m308) worked; this is a different, deeper loop. Made the mock path **self-diagnosing** instead of silently bouncing: (1) `phases/cardplay.js` — routed all 3 mount "redirect to login" recovery points through a new `cardplayRecoveryRedirect(reason)`. In ARTEMIS mock mode (`window.risqueArtemisAutoSave` set) it now shows the exact reason + gameState summary (currentPlayer / players / turnOrder) on-screen + console and **halts (no redirect)** — so the tester sees WHY instead of ping-ponging to login. (2) `js/artemis-auto-save.js` `navigateCardplay` — a full page reload on the host IS the sign-in trap (game already started server-side → host can't rejoin → login); the host now **never full-reloads** during mock boot: if `risqueNavigateGameHtmlSoft` is refused it logs a loud diag (`isPublic`, `softAllowed`) + on-screen error rather than looping. **Next step:** user re-tests mock+Mictor on m309 and reports the on-screen/console reason so we can fix the root cause (suspect: host is not the active player when a client starts — spectator-host mock mount, or `risqueDisplayIsPublic` state on host). Files: `phases/cardplay.js`, `js/artemis-auto-save.js`, `game.html`, `launchers/profiles.json`. Tab title **ARTEMIS m309**.
- **2026-07-02 m308** — Fixed the real cause of the host "sign-in loop": the low-left **wayback "SAVE FOLDER NOT CONNECTED" badge** (`#risque-wayback-connected-flag`, inline `z-index:2147483646`, `pointer-events:none`) floated *over* the launcher's **Launch** button, so the host couldn't click it and resorted to `Ctrl+Shift+R` to escape — which (per m-note 2026-06-28) always dumps the host to sign-in because the game is already started server-side → endless loop. Fix (CSS only): hide that badge while the menu is open (`html.risque-artemis-host-launcher-active #risque-wayback-connected-flag { display:none !important }`) and raise the launcher overlay to `z-index:2147483647` so nothing can overlap it. No JS/logic change — the m307 launcher + beat-gate reorder stand. Files: `game.css`, `game.html`, `launchers/profiles.json`. Tab title **ARTEMIS m308**. **Reminder for user:** don't `Ctrl+Shift+R` on the host — use the menu's Launch button (now visible) or re-run the batch.
- **2026-07-02 m307** — Reworked the host test launcher into a **foolproof 3-mode menu** and fixed the "Launch → bounced to sign-in" bug. **Bug cause:** the beat-sync gate (`risqueArtemisBeatHostGateBeforeStart`) ran *before* the menu opened, so while the host picked options the sync went stale and `commitArtemisRoster` failed → login overlay reappeared. **Fix:** menu opens first, then sync + start run as the launcher's continuation (`launchWithChoices`). Added a re-entry guard (`risqueArtemisHostLauncherIsOpen`) so START polling can't reset an open menu. **New menu:** three exclusive modes — **1 Normal play** (full setup, fair random), **2 Rigged** (full setup → sub-pick who wins selection: Guido/Mictor/Nooch), **3 Load mock game** (round-4 cardplay → sub-pick who starts). Only the relevant sub-panel shows; Launch is disabled until a mode is chosen and its label previews the choice. Mock mode sets `window.risqueArtemisAutoSave='cards'` so it loads even without the URL param; normal/rigged clear it. Files: `js/artemis-login.js`, `game.css`, `game.html`, `launchers/profiles.json`. Tab title **ARTEMIS m307**.
- **2026-07-02 m306** — Host **test launcher** after START (before mock boot or full setup). Guido clicks START → sync beat → overlay with three sections: **Game path** (mock `cards.json` vs full welcome→roulettes), **Setup roulette rig** (true random / rig Guido·Mictor·Nooch — also used if mock load fails), **Mock — first to act** (Guido / Mictor / Nooch). Choices stored in `sessionStorage` (`risqueArtemisHostLaunchMode`, `risqueArtemisMockFirstSlot`); mock path no longer auto-skips the menu when `artemisAutoSave=cards` is on the URL. `artemis-auto-save.js` rotates `turnOrder` + sets `currentPlayer` from launcher pick. Full-setup path skips the duplicate rig picker when rig was already locked in the launcher. Files: `js/artemis-login.js`, `js/artemis-auto-save.js`, `js/artemis-setup-flow.js`, `game.css`. Hard-refresh all three laptops — tab title **ARTEMIS m306**.
- **2026-06-28 m305** — THE fix for the active player's truncated processing shelf (supersedes m301-m303). Decisive evidence: user reported the HOST (Guido, now active first) shows only a brief lower-window scrollbar then enlarges/fits, while CLIENTS truncate — "server is unique". Root cause: any active player (host OR client) mounts the processing shelf with root class `--host-cardplay-recap` (`risquePublicRecapShelfHudClass()` returns host-recap whenever `risqueDisplayIsPublic` is false). The generic `html.risque-view-host … --host-cardplay-recap .risque-public-cardplay-recap-panel--shelf` rule (~game.css:9931) sets `min-height: min(720px, 72vh)` — TALLER than the HUD column. The host has a fitting override `min-height: 120px !important` in the `html.risque-artemis-host … --host-cardplay-recap` refinement block (~13978-14138) so its shelf fits; the CLIENT (`html.risque-artemis-client`) never matched that block, kept the 720px/72vh min-height → shelf overflows the column → lower pane scrollbars + processing card truncates. m301-m303 missed: they targeted `--public-cardplay-recap` (active client never uses it) / host-only scope. Fix (CSS only, end of `game.css`, replaced the dead m303 block): re-apply the host's `--host-cardplay-recap` refinements to `html.risque-artemis-client … --host-cardplay-recap` — esp. the `panel--shelf min-height:120px !important` cap, plus `#hud-main-panel` fill (min-height 200px panel-only), overlay min-height 140px, hide `#hud-stats-panel`/`.hud-phase-player-row`/`#risque-phase-content` (non-gate), title-stack, 3-up card sizing (`max-height: min(28vh,200px)`). Combined with m301 (upper card scales) the client shelf now fits like the host. Verify active player's tab title reads **ARTEMIS m305**.
- **2026-06-28 m304** — Mock cardplay test now starts with **Guido** (host) to act, not Mictor — easier to paste URLs and test from the server machine. `cards.json` regenerated (`turnOrder`: GUIDO→MICTOR→NOOCH, `currentPlayer`: GUIDO); `scripts/gen-cards-save.js` updated; auto-save banner/label text in `js/artemis-auto-save.js` and `START-ARTEMIS.bat` updated. Bumped `profiles.json` `v`/`cacheVersion` and `game.html` to m304.
- **2026-06-28 m303** — THE actual fix for the active player's collapsing cardplay processing shelf (m300/m301/m302 all missed the target — user "saw no change" each time). Decisive finding: the ACTIVE NET CLIENT (Mictor) mounts the recap/processing shelf with root class **`runtime-hud-root--public-cardplay-recap`** (same overlay as the TV), and her `<html>` is **`risque-artemis-client`** (+ `risque-view-host` healed for toggles). But EVERY public-recap sizing/chrome rule in `game.css` was scoped only to `.risque-view-public` or `html.risque-artemis-host.risque-view-host` — so on the active client NONE applied: her `#risque-phase-content` (the compact selection panel) stayed visible and fought the overlay for space, the HUD column was not bottom-pinned, and the two shelf panes had no balanced 50/50 floor. Result on CONFIRM: windows shrink + float up leaving dead space below, lower pane gets scrollbars, processing card truncates ~20%. m301 (`.risque-public-cp-shelf-*` upper-card scale) and m302 (`--host-cardplay-recap` pane floor) targeted classes the active CLIENT never uses, hence no visible effect. Fix (CSS only, end of `game.css`): added a consolidated `html.risque-artemis-client #runtime-hud-root.runtime-hud-root--public-cardplay-recap …` block mirroring the public-TV treatment — bottom-pin the column, hide `#risque-phase-content`/`#hud-stats-panel`/`.hud-phase-player-row`/`#risque-main-panel-body`, flex-fill `#hud-main-panel` (25px inset, no green plate), fill overlay+panel, and steady equal 50/50 panes (`flex:1 1 0; max-height: calc((100%-gap)/2 - 10px)`) for the whole animation. Combined with m301 (upper card scales to pane) the card no longer truncates. Also bumped `launchers/profiles.json` `v`/`cacheVersion` and `START-ARTEMIS.bat` label to m303 so the batch flow forces fresh assets. **Cache note:** server auto-reloads `profiles.json` (mtime) + reads `active-mode.txt` per request, so NO server restart needed; but the host browser must FULLY reload (close tab/window) — Ctrl+Shift+R works on clients, and on the host it dumps you to sign-in (re-run batch instead). Verify the active player's tab title reads **ARTEMIS m303** before judging the result.
- **2026-06-28 m302** — Active player's cardplay processing shelf collapsed on CONFIRM (Mictor). User test (watching Mictor only): pre-confirm the lower window + 3 cards look correct; on CONFIRM both shelf windows shrink in height and rise up leaving dead space below, the lower pane gets unwanted scrollbars, and as cards animate up the upper card truncates ~20% at the bottom. Root cause: the recap shelf's **persistent** 50/50 pane floor (`flex:1 1 0; min-height: min(260px,30vh)` on `.risque-public-cp-shelf-upper/-lower`) was scoped to `html.risque-artemis-host.risque-view-host` — i.e. only the hot-seat host (Guido). The active **net client** (Mictor) is `risque-view-host` (added by the artemis-play heal so she gets toggles/SKIP) but is NOT `risque-artemis-host`, so she only received the floor from the `:has(.risque-public-cp-shelf-upper-content:empty)` rules. The instant a card flew into her upper pane that `:empty` guard stopped matching, both panes lost their floor and collapsed to content height → shrink + float-up + lower scrollbars + truncated processing card. Fix (CSS only, `game.css`): added a `body.risque-view-host #runtime-hud-root.runtime-hud-root--host-cardplay-recap .risque-public-cp-shelf-upper/-lower` branch to the persistent floor rule so the active client keeps steady, full-size 50/50 panes throughout the whole animation (matches Guido/hot-seat). Does not touch the public/TV recap (`--public-cardplay-recap`) or observers. Combined with m301 (upper processing card scales to the pane) cards no longer truncate. Bumped `game.css` cache + build meta/title to m302; reload with **Ctrl+Shift+L** (or re-run the batch) — NOT Ctrl+Shift+R, which dumps the host at sign-in. **IMPORTANT cache gotcha (found while testing):** `launchers/profiles.json` pinned the cardplay-test join URLs to `v=m299`. Because that `v` tag is part of the `game.html` URL and never changed between runs, the browser kept serving a cached `game.html` that still referenced the OLD `game.css` link — so m300/m301/m302 CSS never loaded and the user "saw no difference". Fix: bump the `v` param (and `cacheVersion`) in `profiles.json` to the current build whenever you ship CSS/JS, so the batch flow forces a fresh `game.html`. Also confirmed: the ACTIVE player mounts the recap shelf (`--host-cardplay-recap`, via `risquePublicEnsureCardplayRecapPanel` with `!risqueDisplayIsPublic`) for the post-confirm processing animation — so the m301/m302 `.risque-public-cp-shelf-*` edits are the correct target for her view (NOT the `.cardplay-hand-staging-split` compact root, which is only the pre-confirm selection UI).
- **2026-06-28 m301** — Cardplay PROCESSING shelf truncation fix (all players, animation). User clarified: during card-processing animation every player should see a two-pane shelf of **equal** size — cards to process in the bottom pane, then one at a time fly to the top pane and get processed — modeled on the receive-card layout, but cards in the top window were truncated and sizing "varies between players". Root causes (both in the recap **shelf**, `.risque-public-cardplay-recap-panel--shelf`, which is the processing view shown to host/observers/active): (1) the **upper processing card** uses `.risque-public-cardplay-recap-card-img` locked at a fixed `186px`, while the **lower** waiting cards (`.risque-public-cp-shelf-card`) already scale with `min(28vh, 186px)` — so the top card overflowed and clipped whenever the top pane was shorter than one card; this hit every player processing. (2) the **host** panes were unequal: lower pane floored at `min-height: min(340px, 42vh)` vs upper `min-height: 0`, so on shorter screens the lower stayed big and squeezed the upper (the "varies between players" — observers/public panes were already equal). Fix (CSS only, `game.css`): upper processing card now scales like the lower shelf cards (`width:auto; max-width:120px; height:auto; max-height:min(26vh,186px)`; its card-row is `flex:1 1 auto; min-height:0; align-items:center`), and the host lower-pane floor dropped to `min-height: 0` to match the upper so both render as equal halves. Observer view unchanged (its panes were already equal and its cards already scaled). Mock cardplay-test boot (m299) left as-is. Bumped `game.css` cache + build meta/title to m301; hard-refresh all three laptops (or re-run the batch flow).
- **2026-06-28 m300** — Cardplay UPPER hand pane truncation fix (active players only). User report: every player *processing* cards in cardplay saw the thumbnails in their upper window cut off, while observers were fine. Cause: the active player's compact two-pane split (`.cardplay-hand-staging-split`) gives the upper `.cardplay-hand-stack` and lower `.cardplay-staging-wrap` an equal `flex: 1 1 0`, but the hand thumbnails are a fixed `max-height: 186px` and the hand row was `flex: 0 1 auto` with `overflow: visible` + 25px top padding — so whenever the 50% pane was shorter than one full card, the card overflowed and got clipped by the compact root's `overflow: hidden`. Observers were unaffected because their post-confirm recap shelf scales its single card. Fix (CSS only, `game.css`, panel-only scope so spectators/host recap untouched): hand row now `flex: 1 1 auto; min-height: 0; overflow: hidden; padding-top: 10px`, hand-stack `overflow: hidden`, and the compact card thumbnails now scale to the pane (`width:auto; max-width:120px; height:auto; max-height:100%`) like the receive-card panes — so a full card always fits and the upper window reads the same height as the lower staging window. Mock cardplay-test boot (m299) left as-is for easy testing. Bumped `game.css` cache + build meta to m300; hard-refresh all three laptops (or re-run the batch flow) to pick it up.
- **2026-06-28 m299** — Idiot-proof cardplay test boot (no keyboard shortcuts). User runs the usual `START-ARTEMIS.bat` + client `JOIN.bat` only. `START-ARTEMIS.bat` now sets `launchers/active-mode.txt` to `cardplay-test`; `launchers/profiles.json` routes all three join URLs to profiles with `artemisAutoSave=cards` and `v=m299` (cache bust). New `js/artemis-auto-save.js`: after fast-boot login on every machine, fetches `cards.json` and jumps straight to round 4 cardplay with Mictor to act and a valid book in each hand — skips welcome/deal/setup entirely. Uses `artemisAutoSave` (NOT `artemisPreset`) so `artemisPresetCardplayActive` host-reject does not block commits. Host pushes mirror after load; clients load the same save locally then follow mirror. Status banner: "CARDPLAY TEST — Mictor's turn — play the book…". Diagnostic `auto-save-boot` in session log. To return to normal full game: change `active-mode.txt` back to `normal` or edit `START-ARTEMIS.bat` line that writes `cardplay-test`. Bumped caches in `game.html`; restart server batch so join URLs pick up m299.
- **2026-06-28 m298** — Cardplay "cards still in hand" — fast-repro mock save + decisive instrumentation + broadened guard (m297 still didn't fix it; latest `logs\artemis-session.jsonl` showed Mictor's `handCards/cardCount` stuck at 3 through the recap push with NO `cardplay-apply-regress-block` firing, so the revert is NOT going through the m297 chokepoint — likely the cardplay→income handoff where the incoming mirror is phase=income, which m297's phase gate excludes). Three deliverables: (1) **`cards.json`** mock save at repo root — round 4, `phase:cardplay`, `currentPlayer:MICTOR`, every player holds a valid 3-of-a-kind (all-infantry) book; all 42 territories owned; carries **no** preset id so it behaves like a real dealt game (avoids the `artemisPresetCardplayActive` host reject path). Generated by `scripts/gen-cards-save.js`. (2) **Loader `Ctrl+Shift+J`** (`js/game-shell.js`, `window.risqueArtemisLoadMockSave`) — fetches `cards.json`, writes it to `localStorage.gameState`, then reuses the m294 dev-reload path so a live networked session jumps straight into round-4 cardplay. Trigger on host first, then the active player. (3) **Diagnostics** to pinpoint removal-vs-revert on the next (now-seconds-long) test: `cardplay_book_removed` (before/after hand + discard size, `phases/cardplay.js`), `handCount`/`bookPlayedThisTurn` added to `cardplay_host_recap_push` (what the client actually pushes), `cardplay-host-accept` + `handCount` on `cardplay_host_income_advance` (`js/artemis-net.js`, what the host receives/adopts), `cardplay-apply-trace` / `cardplay-apply-handpreserve` (`js/game-shell.js`, every active-client mirror apply during cardplay/income), and a one-shot `artemis-build-loaded` boot marker so logs unambiguously show the live build per machine. Plus a **broadened own-hand preservation** in `risquePublicMirrorGameStateApply`: for the active net client whose turn it still is and who has committed a play this turn (`bookPlayedThisTurn` or recap seq > 0), an incoming cardplay/con-cardplay/income/con-income mirror that would ADD cards back onto our own committed hand is patched (our committed cards kept) instead of vetoed, so the recap push still carries the reduced hand. NOTE: this protects the active client's local view + push; if the host toggle still shows the cards for other players after this build, send the new log lines — `cardplay_book_removed` → `cardplay_host_recap_push handCount` → `cardplay-host-accept hostCards/inCards` chain will show exactly where the reduction is dropped. Bumped cache for `game-shell.js`, `artemis-net.js`, `cardplay.js`; hard-refresh all three laptops.
- **2026-06-28 m297** — Cardplay book "never leaves hand" — TRUE root cause + hard guard. m296 wasn't enough: read `logs\artemis-session.jsonl` after a Mictor book, and the income-load console showed his state wholesale reverted to PRE-book (`bookValue:0`, `bookPlayedThisTurn:false`, full 3-card hand incl. the wildcard he'd traded), while `cardplay_client_snapshot` held `handCards:3 cardCount:3` the entire turn with `clientPlaying:true` and NO `cardplay_hand_hydrate` — i.e. a stale pre-play public mirror was replacing `window.gameState`, slipping past the m296 owner guard (which sits AFTER the control-slot mismatch branch that force-applies). Fix: a hard regression guard at the single apply chokepoint `risquePublicMirrorGameStateApply` (`js/game-shell.js`, where `window.gameState = gs`). For an active net client in cardplay/con-cardplay whose name matches both local and incoming currentPlayer, the apply is VETOED when the incoming mirror would regress the player's own committed play — i.e. gives them MORE cards than local, or flips `bookPlayedThisTurn` true→false, or lowers `risquePublicCardplayRecapAckRequiredSeq`. Legit turn-advance (incoming names another player), income transition (incoming phase=income), and spectator/host views are unaffected. Logs a throttle-free `cardplay-apply-regress-block` diag when it vetoes. Kept m296 owner protection + m295 host guard as layered defense. Bumped `game-shell.js` cache; hard-refresh all three laptops.
- **2026-06-27 m296** — Cardplay book "never leaves hand" ROOT-CAUSE FIX (confirmed by m295 diagnostics). The `logs\artemis-session.jsonl` `cardplay-mirror-clobber-risk` markers fired repeatedly during the active client's OWN cardplay turn (e.g. slot 3 NOOCH, phase cardplay, localCurrent=incomingCurrent=NOOCH, 5× in a row), proving the host wasn't rejecting — the active client was applying the host's frozen pre-play mirror because `risqueArtemisClientPlaying` transiently drops to false (panel re-mount churn), restoring the just-played cards (and causing the staging↔hand "bounce"). Fix in `risqueArtemisClientShouldApplyPublicMirror` (`js/game-shell.js`): in the cardplay branch, immediately after the currentPlayer/control-slot mismatch check (which already handles genuine turn-advance), if the incoming mirror still names US as the current player we own cardplay and now return `false` (do not apply) regardless of the flaky `risqueArtemisClientPlaying`/controls flags. Spectators/host are unaffected (`risqueArtemisClientNameMatchesCurrent` returns false for host/public). Replaced the m295 `cardplay-mirror-clobber-risk` diag with a throttled `cardplay-mirror-protect` diag (once per control seq) confirming the guard engaged. Kept the m295 host-side empty-hand guard exemption as defense-in-depth. Bumped `game-shell.js` cache; hard-refresh all three laptops.
- **2026-06-27 m295** — Cardplay book/card "never leaves hand" investigation + first fix. Root finding: the local removal works in every play path (`currentPlayer.cards` is spliced); the cards come back only via the host↔client sync layer. Prime suspect for a **book** (which can empty the hand): the host empty-hand guard in `applyHostClientState` (`js/artemis-net.js`) rejected any incoming state that wiped the current player's hand unless the sender was recognized as the active slot — so a legitimate end-of-cardplay recap commit that traded the last cards into a book got dropped when the host control-slot was momentarily stale, leaving the host on the old hand and re-broadcasting it. Fix: never reject when the incoming state is a fresh, higher `risquePublicCardplayRecapAckRequiredSeq` for the same `currentPlayer` (active-player recap commit). Added two diagnostics (read via `logs\artemis-session.jsonl`) to confirm root cause on the next playtest: `cardplay-guard-reject` / `cardplay-guard-allow` (host, `js/artemis-net.js`) and `cardplay-mirror-clobber-risk` (active client applying the host mirror because `risqueArtemisClientPlaying` was false, `js/game-shell.js`). If a book still sticks after this build, send `logs\artemis-session.jsonl` — those lines pinpoint host-reject vs. client-clobber. Bumped `game.html` cache for `artemis-net.js` + `game-shell.js`; hard-refresh all three laptops.
- **2026-06-27 m294** — Added ARTEMIS dev reload shortcut: `Ctrl+Shift+L` (`js/game-shell.js`). It ignores text inputs, saves the current host `gameState` when appropriate, stamps ARTEMIS session-resume flags in `sessionStorage`, rebuilds the URL with the live `gameState.phase` (including deploy `kind`), adds an `artemisDevReload=<timestamp>` cache-bust param, and navigates so host/client laptops can reload updated JS/CSS without restarting the game/lobby. Exposed as `window.risqueArtemisDevReload`. Updated top-bar hint and bumped `game-shell.js` cache in `game.html`; hard-refresh once to load m294, then use `Ctrl+Shift+L` for same-phase dev reloads.
- **2026-06-27 m293** — Four changes. (1) Deployment buttons (RESET / +2 / +5 / +10 / ALL / PROTECT-ALL / CONFIRM) ~50% larger for all players: base 14→21px, host HUD row 17→25px, host protect-all 11→16px, deploy-dock base 14→21px, deploy-dock host row 12→18px (`game.css`). (2) Receive-card Control Voice no longer prematurely shows "TOTAL CARDS IN HAND = N" on arrival — the tally is now set *after* the staging-merge flags, so it stays hidden until the card flies up (merge), then reveals via `receiveCardSetMessage("")` in `receiveCardScheduleAdvanceAfterStagingMerge`; observers' CV follows automatically via `risqueControlVoice` mirroring (`phases/receivecard.js`). (3) Observer (Guido/Nooch) receive-card panes now grow into two equal windows like the active player instead of collapsing (`runtime-hud-root--receivecard-panel-only` `.risque-public-receivecard-pane` / `-staging-wrap` → `flex: 1 1 0`, no `margin-top: auto`), which also gives the fly-up a real target window so the card no longer flies into empty space (`game.css`). (4) Campaign attack (instant + step): the FINAL acquired territory no longer auto-locks its troop transfer — `applyBattleRoundAfterRoll` detects the last hop and hands the attacker the normal manual transfer slider (resets campaign planning, re-enables attack chrome); the instant + step run loops detect `campaignFinalManualTransfer` and skip the success banner so CONFIRM resumes normal attack flow (`phases/attack.js`). Q-dev silent runs keep auto leave-1.
- **2026-06-25 m292** — Completes the m291 dice-capture fix. The active client's only attack-live push fired *before* the capture transferred ownership, so spectators never received the post-capture board until the transfer was confirmed. Added a `pushArtemisClientAttackLiveMirror()` right after `attackPhase = 'pending_transfer'` so the server relays the captured territory (attacker-owned, min troops) to host + other clients immediately (`phases/attack.js`).
- **2026-06-25 m291** — Two fixes. (1) Dice attack capture: observers (host + other clients) now see the captured territory flip to the attacker with the minimum auto-moved troops in real time. `applyAttackLiveSpectator` only updated troops of existing territories; it now transfers ownership of the acquired territory, pinned to min troops, with the source pinned to its post-capture snapshot so the attacker's additional-transfer preview stays hidden until CONFIRM (`js/artemis-net.js`). (2) Observer post-cardplay recap shelf opens as balanced 50/50 windows from first paint instead of the lower window opening oversized and resizing mid-animation (`game.css`).
- **2026-06-25 m290** — Receive-card phase (active host + clients): make the two panes ("Cards in hand" / "Received card") grow into equal halves instead of content-sized. Root cause: `runtime-hud-root--receivecard-panel-only` rule used `flex: 0 1 auto !important`, overriding the host's intended equal-split. Now `flex: 1 1 0`, min-height 0, max-height half; continue button keeps its own room below. CSS-only (`game.css`).
- **2026-06-25 m289** — Host cardplay recap shelf: keep upper/lower panes balanced while the upper pane is still empty (card staging). Removes the lower-window ballooning that snapped back to correct proportions once the card flew up. CSS-only (`game.css`).
- **2026-06-10 m288** — Client cardplay CONFIRM: push recap + board/hand to host (`player_state`); host accepts active player by roster name (not stale controlSlot); active client rejects stale mirror that rewinds hand; flush public mirror on recap publish.
- **2026-06-10 m287** — Host deploy when not first in order (e.g. Mictor rig P2 deploys first): fix owner slot conflating turn position with roster slot; Guido gets wheel/confirm on his deploy turn.
- **2026-06-10 m286** — Clients keep fullscreen on phase advances (attack, income, deploy, etc.): ARTEMIS net clients may use same-document soft navigation instead of full page reload.
- **2026-06-10 m281** — Campaign Step on Guido: revert m279 leave-dock split; restore L1/L3 inside CV; drop host 2× button scaling; wire Commit/Begin/Reset onclick; CV min-height 240px. Guido host attack dice timing ~520ms (m280) confirmed good.
- **2026-06-10 m285** — Fix m284 regression: income teardown must not `hidden` entire attack chrome (blitz/campaign toolbar). Restore chrome on attack entry; hide only dice columns off-attack.
- **2026-06-10 m284** — Guido host: tear down attack-spectator dice when phase leaves attack (income/cardplay/deploy); CSS scoped `risque-artemis-attack-spectator` dice to `data-risque-phase="attack"` only.
- **2026-06-10 m283** — Spectator mirror fixes: reinforce CV title-only until source picked (no idle prompt / stale “Next player”); Guido +N reinforce wheel badge; attack transfer confirm flushes map to host/clients immediately; clear stale receive-card/cardplay CV backs during cardplay waiting.
- **2026-06-10 m282** — Cardplay spectator CV: grow when card backs in voice (fix 168px clip after receive-card→cardplay); clear receive-card flyer on cardplay paint. Income CV: remove 50vh cap + overflow-x/y pair (scrollbar bug); panel grows with breakdown.
